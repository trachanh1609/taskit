$(function() {
  const model = {
    init () {


    }
  };

  const controller = {
    init () {
      model.init();
      view.init();
    }
  };

  const view = {
    init () {

    }
  };

  controller.init();

});
